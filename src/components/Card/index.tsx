import { Box, Typography } from '@material-ui/core';
import React from 'react';

const Card: React.FC = () => {
  return (
    <Box>
      <Typography></Typography>
    </Box>
  );
};

export default Card;
